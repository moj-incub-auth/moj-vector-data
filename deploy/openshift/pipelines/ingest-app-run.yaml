apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  generateName: ingest-app-piperun-
spec:
  pipelineRef:
    name: ingest-app-pipeline
  serviceAccountName: pipeline 
  workspaces:
    - name: shared-data
      volumeClaimTemplate:
        spec:
          accessModes:
            - ReadWriteOnce
          resources:
            requests:
              storage: 40Gi