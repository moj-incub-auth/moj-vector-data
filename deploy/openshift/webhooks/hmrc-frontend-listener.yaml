apiVersion: triggers.tekton.dev/v1alpha1
kind: EventListener
metadata:
  name: hmrc-frontend-listener
spec:
  triggers:
    - bindings:
        - ref: git-repo-binding
      template:
        ref: hmrc-frontend-template