kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: el-ingestion-app-listener
  namespace: moj-builder
spec:
  to:
    kind: Service
    name: el-ingestion-app-listener
    weight: 100
  port:
    targetPort: http-listener
  wildcardPolicy: None