kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: el-search-app-listener
  namespace: moj-builder
spec:
  to:
    kind: Service
    name: el-search-app-listener
    weight: 100
  port:
    targetPort: http-listener
  wildcardPolicy: None