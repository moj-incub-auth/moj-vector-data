kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: el-vibe-ui-app-listener
  namespace: moj-builder
spec:
  to:
    kind: Service
    name: el-vibe-ui-app-listener
    weight: 100
  port:
    targetPort: http-listener
  wildcardPolicy: None